---
layout: submission
title: "Submission by 0xec3281124d4c2fca8a88e3076c1e7749cfecb7f2 for challenge Sort"
submitter: "0xec3281124d4c2fca8a88e3076c1e7749cfecb7f2"
wild: True
challenge: Sort
gas: 538862
submitted: 2018-06-08 03:03:17.522608+00:00
---
```solidity
pragma solidity 0.4.24;


contract Sort {

    function sort(uint[] input) public pure returns(uint[]) {
        if (input.length == 0)
            return;
        uint arrayStart = 0;
        assembly {
            arrayStart := add(input, 0x20)
        }
        sort(arrayStart, input, 0, (input.length-1)*0x20);
        return input;
    }

    function sort(uint arrayStart, uint[] arr, uint left, uint right) internal pure {
        uint i=left;
        uint j=right;
        uint pivot;

        assembly {
            pivot := mload(add(arrayStart, mul(0x20, add(div(left, 0x20), div(sub(div(right, 0x20), div(left, 0x20)), 2)))))
            for {} iszero(gt(i, j)) {} {
                for {} lt(mload(add(arrayStart, i)), pivot) {} {
                    i := add(i, 0x20)
                }

                for {} lt(pivot, mload(add(arrayStart, j))) {} {
                    j := sub(j, 0x20)
                }
                switch gt(i, j)
                case 0 {
                    let iLoc := add(arrayStart, i)
                    let jLoc := add(arrayStart, j)
                    let temp := mload(iLoc)
                    mstore(iLoc, mload(jLoc))
                    mstore(jLoc, temp)
                    i := add(i, 0x20)
                    j := sub(j, 0x20)
                }
            }
        }
        if (left < j)
            sort(arrayStart, arr, left, j);
        if (i < right)
            sort(arrayStart, arr, i, right);
    }
}

```
